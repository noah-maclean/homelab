# Home Assistant

- ran helper script with advanced settings to set id to 200 (first vm) and name to homeassistant-vm
- set ip to static in home assistant dashboard (Settings -> System -> Network -> Configure network interfaces -> IPv4)
	- set to 192.168.1.25 (next free)
	- should i set it to 192.168.1.30? (separate vm from lxc)
	- or 31 (first lxc is 21) !! this one
- google home devices, philips hue and bt home hub show up
- think google homes need an ssl certificate/external domain to be fully featured
- philips hue needs the bridge to be pressed
- set up lamp (Tuya)
	- Add Device -> Tuya -> follow steps
- detected thread?
	- is that from hue bridge
- connected hue bridge (one button press and most rooms automatically assigned)
- [added Google Home Hub as thread border router](https://www.home-assistant.io/integrations/thread#case-2-creating-a-home-assistant-border-router-when-there-is-an-existing-network)
- enabled Matter add on
- hopefully Matter over Thread devices will work (like Ikea Kajplats)

- enabled Tailscale add on to allow external access (needs https)
- turn off advertise as an exit node
- turn off advertise as app connector
- turn on share with serve
- hit save!!
- disable key expiry in tailscale admin console

- installed Studio Code Server add on to change Tailscale settings through web ui
- add the following to configuration.yaml
```yaml
http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 127.0.0.1
```

- now,
    - add home network in homeassistant home settings
    - add tailscale magicdns address to homeassistant app Home Assistant URL
    - add ip address as Internal Connection URL
    - change remote connection security level to most secure

