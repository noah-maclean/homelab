# 22/06/2026

## Hermes

- asked hermes to setup web dashboard
- using basic auth on lan (username and password)
- on sessions page, dashboard shows connected platforms and recent sessions
- asked hermes to add active sessions above recent ones
- needed a restart of the dashboard, but got hermes to do that

## Glance

- installed [glance agent](https://github.com/glanceapp/agent) on rasppi via docker
- docker-compose.yml:

```yaml
services:
  glance-agent:
    container_name: glance-agent
    image: glanceapp/agent
    environment:
      # Optional, leave empty or comment out for no authentication
      TOKEN: <generated_token>
      HIDE_MOUNTPOINTS_BY_DEFAULT: "true"
      MOUNTPOINTS: "/host"
    volumes:
      # Add your mount points below in the format host_path:container_path with :ro suffix for read-only
      - /:/host:ro
    ports:
      - 27973:27973
    restart: unless-stopped
```

- added server stats for rasppi to proxmox glance config

```yaml
- type: server-stats
  servers:
    - type: remote
      name: RaspPi
      url: http://192.168.1.10:27973
      token: <generated_token>
```

- token generated using 1Password password generator

- added hermes to monitor services
- added icon from dashboard-icons (di) as selfh.st didn't have an icon (needs to be svg)
- added icon directly as png (<https://cdn.jsdelivr.net/gh/selfhst/icons/png/hermes-agent.png>)
