# 19/06/2026

## Crafty Controller

- server kept crashing when trying to generate chunks with chunky
- added `-Dchunky.maxWorkingCount=128` in startup arguments (default is 512)
- another error happened, so removed ./world/voxyserver folder and continued chunky generation
- changing from Java 25 to Java 21 JRE didn't work as it is no longer supported in this MC version
- changed the argument `-XX:+UseZGC` to `-XX:+UseG1GC` to use standard garbage collection
       - startup command: `java -Xms1000M -Xmx3000M -XX:+UseG1GC -Dchunky.maxWorkingCount=128 -jar fabric.jar nogui`
- continued to crash, so increased minimum ram to match max to prevent memory resizing and changed garbage collector again
       - `java -Xms3000M -Xmx3000M -XX:+UseParallelGC -Dchunky.maxWorkingCount=128 -jar fabric.jar nogui`
- getting more ram might also help this issue
- server crashed again, but not because of garbage collecter
- installed openjdk-25-jre-headless to use instead of temurin version
- switched override current java version to this newly installed version
- STILL DIDN'T WORK
- download graalvm - still didn't help (different crash)

- switched back to original java version (temurin) and decreased ram allocation in server commands (-Xms2048M -Xmx2400M)

- whole server went down and needed a manual restart

- changed `updateInterval` from 1 to 20 in config/chunky/chunky.json
- continued to crash, so created a new server to test

### New Server

- started with no mods
       - crashed immediately
- changed execution command to
       - `java -Xms3000M -Xmx3000M -XX:+UseParallelGC -XX:-OptimizeStringConcat -jar fabric.jar nogui`
- crashed immediately again, so changed command to this
       - `java -Xms3000M -Xmx3000M -XX:+UseParallelGC -XX:TieredStopAtLevel=1 -jar fabric.jar nogui`
- server booted without crashing
- now to add mods
       - chunky
       - cloth config
       - entity culling
       - fabric api
       - ferrite core
       - lithium
       - no chat restrictions
       - sodium
       - voxy
       - voxy server
- working fine
- generate chunks

```shell
chunky shape circle
chunky radius 256c // 256 chunks
chunky spawn
chunky start

```

- chunk generating super slow but not crashing 10 mins in
       - eta up to 20 hours
- will need to run `voxyserver import existing all` to load chunks into voxy

- NO crashed again
- will try again after getting more ram and allocate at least 4-6GB
